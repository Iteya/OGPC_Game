/// @description Attack

/* currently broken
if (!isAttacking) // the player cannot start attacking if already in the process
	{
		if (weapon == "bow") //if the player is using a bow
			{
				oBow.sprite_index = sBowFiring
				isAttacking = true
				arrowDirection = arctan((mouse_x - x) / (mouse_y - y))
			}
	}






